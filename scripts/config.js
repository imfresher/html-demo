export const APP_ENV = 'development';

export const IS_PRODUCTION = (APP_ENV === 'production');
export const IS_DEVELOPMENT = (APP_ENV === 'development');
export const IS_TESTING = (APP_ENV === 'testing');
